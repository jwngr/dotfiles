_install_xspec()
{
    local xspec=$1 cmd
    shift
    for cmd in $@; do
        _xspecs[$cmd]=$xspec
    done
    complete -F _filedir_xspec $@
}
if type -t _filedir_xspec 1>/dev/null 2>/dev/null; then
    declare -A _xspecs
    _install_xspec '!*.@(Z|[gGd]z|t[ag]z|bgz)' gunzip zcat unpigz
fi
